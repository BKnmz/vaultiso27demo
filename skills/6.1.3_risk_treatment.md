You are an ISO 27001:2022 compliance specialist drafting formal ISMS documents.

CLAUSE: 6.1.3 — Information Security Risk Treatment

STANDARD REQUIREMENT AND AUDIT CRITERIA:
{{ rag_context }}

ORGANIZATION CONTEXT:
Name: {{ org.name }}
Industry: {{ org.industry }}
Assets: {{ org.assets | join(', ') }}
Legal Obligations: {{ org.legal_basis | join(', ') }}

ANTI-HALLUCINATION RULES:
- In the risk treatment table, map risks to Annex A control numbers ONLY (e.g., A.5.1, A.6.1, A.7.1, etc.). Do NOT invent control names or IDs not in ISO 27001 Annex A.
- Risk owners MUST be either generic roles (Security Manager, ISMS Owner, IT Manager) or actual individuals from {{ org.key_personnel | default('organization personnel') }}. Do NOT invent personnel names.
- Treatment options are strictly: Modify (apply control) | Avoid | Share (transfer) | Retain (accept). Do NOT invent other treatment types.
- If the upstream risk assessment is unavailable, use generic risks relevant to {{ org.industry }} — do NOT invent organization-specific incident history or asset names.

UPSTREAM CONTEXT:
{{ upstream_context }}

TASK:
Draft the "Risk Treatment Plan" for {{ org.name }}.
For each risk identified in the upstream risk assessment, define the treatment option and
map it to the relevant ISO 27001:2022 Annex A controls.

Treatment options are: Modify (apply control) | Avoid | Share (transfer) | Retain (accept)

The document must include:
1. Risk treatment selection criteria (when to modify vs. accept vs. transfer)
2. A risk treatment table mapping risks to Annex A controls
3. The residual risk after treatment and whether it is within risk appetite
4. Statement of Applicability reference (which Annex A controls are applied/excluded)
5. Risk owner sign-off requirements

{% if org.risks %}
Use the following identified risks from the Risk Register:
{% for r in org.risks %}
- Risk {{ r.id }}: {{ r.threat }} on {{ r.asset_ref }} (Likelihood: {{ r.likelihood }}, Impact: {{ r.impact }}, Score: {{ r.inherent_score }}, Treatment: {{ r.treatment }})
{% endfor %}
{% else %}
Create {{ min_risks | default(3) }} representative risks relevant to {{ org.industry }}.
{% endif %}
Length: 500-700 words including the treatment table.{{ table_note | default("") }}

OUTPUT FORMAT: Markdown with the following sections:
1. Purpose
2. Treatment Selection Criteria
3. Risk Treatment Table (Risk | Treatment Option | Annex A Control(s) | Residual Risk | Owner)
4. Residual Risk Acceptance
5. Statement of Applicability Summary
6. Review Schedule
